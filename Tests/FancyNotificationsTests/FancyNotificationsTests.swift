import XCTest
@testable import FancyNotifications

final class FancyNotificationsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FancyNotifications().text, "Hello, World!")
    }
}
